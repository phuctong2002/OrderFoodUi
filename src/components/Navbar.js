const Navbar = ({ children }) => {
  return (
    <div className="flex flex-row">
      <div className="w-[100px] bg-[red] h-screen">
        
      </div>
      {children}
    </div>
  );
};

export default Navbar;
